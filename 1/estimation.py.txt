import streamlit as st

# --- PROJECT ACCESS GUARD ---
if not st.session_state.get("active_project"):
    st.warning("⚠️ **Access Restricted:** Please return to **app.py**, select an active project, or initialize a new development phase first.")
    st.stop()

st.header("🏦 Capital Stack & Commercial Loan Proposal")
st.markdown(f"Target Loan Package for: **{st.session_state['active_project']}**")

st.markdown("""
### Commercial Construction Loan Parameters
* **Borrower Entity:** Wickboldt Capital
* **Target Request Structure:** Initial tranche targeted for primary phase construction tracts (e.g., Tracts C1-3).
* **Underwriting Equity Standard:** Disciplined **30% equity position** contributed by sponsor prior to commercial debt drawdown.
* **Collateral:** First-position mortgage on build-to-rent residential development lots.
""")

st.markdown("---")
st.subheader("Loan Request Summary Builder")
loan_amount = st.number_input("Requested Construction Loan Amount ($)", value=1295000.0, step=25000.0)
interest_rate = st.number_input("Estimated Interest Rate (%)", value=7.5, step=0.25)
loan_term_months = st.selectbox("Loan Term (Months)", [12, 18, 24, 36])

if st.button("Generate Formal Loan Summary Package", use_container_width=True):
    st.success("Loan package parameters formatted successfully for banking presentation.")
    st.metric("Total Debt Facility", f"${loan_amount:,.2f}", f"{interest_rate}% Interest Rate")