import streamlit as st

# --- PROJECT ACCESS GUARD ---
if not st.session_state.get("active_project"):
    st.warning("⚠️ **Access Restricted:** Please return to the **Landing Page** (`app.py`), select an active project, or initialize a new development phase first.")
    st.stop()

st.header("🏠 Executive Dashboard")
st.markdown(f"### Active Project: `{st.session_state['active_project']}`")

col1, col2, col3 = st.columns(3)
col1.metric("Active Development", st.session_state['active_project'], "Build-to-Rent")
col2.metric("Target Equity Position", "30.0%", "Standard Underwriting")
col3.metric("Narrow-Footprint Config", "3 Bed / 2 Bath", "Max Width: 26 ft")

st.markdown("---")
st.markdown("""
### Portfolio Overview & Execution Standards
* **Development Strategy:** Build-to-rent residential single-family housing portfolios prioritizing high energy efficiency and optimized space utilization.
* **Structural Methodologies:** Insulated Concrete Form (ICF) systems combined with rigorous framing standards.
* **Financial Structure:** Standardized commercial underwriting maintaining a disciplined **30% equity position** across all phases.
""")