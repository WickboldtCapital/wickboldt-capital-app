import streamlit as st

# --- PROJECT ACCESS GUARD ---
if not st.session_state.get("active_project"):
    st.warning("⚠️ **Access Restricted:** Please return to the **Home** landing page (`app.py`), select an active project, or initialize a new development phase first.")
    st.stop()

# Replace this title with the specific module name (e.g., Capital Stack & Debt, Due Diligence, etc.)
st.header("Module Title")
st.markdown(f"Active Project: **{st.session_state['active_project']}**")
st.markdown("Module content under development.")